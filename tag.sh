#!/bin/bash

# CURRENT_BRANCH=$(git symbolic-ref --short HEAD)
v=1
v=v+1
echo $v   
# Specify the tag format you want to use
# TAG_FORMAT="$CURRENT_BRANCH-$v"

# # Create a tag with the specified format
# TAG_NAME=$(date +$TAG_FORMAT)
# git tag $TAG_NAME

